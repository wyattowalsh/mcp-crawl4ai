---
title: Architecture
description: Canonical architecture for Crawl4AI-MCP
---

import { Diagram } from '@/components';
import { Callout } from 'fumadocs-ui/components/callout';

# Architecture

Crawl4AI-MCP uses a single-module FastMCP server backed by one lifespan-managed `AsyncWebCrawler`.

<Callout type="info">
  Canonical tool surface: <strong>scrape</strong>, <strong>crawl</strong>, <strong>close_session</strong>, and <strong>get_artifact</strong>.
</Callout>

## System Overview

<Diagram
  definition={`
graph TB
    Client[MCP Client] --"MCP Request"--> FastMCP
    FastMCP --"MCP Response"--> Client

    subgraph "Crawl4AI MCP Server"
        FastMCP[FastMCP v3 Server]
        Lifespan[Lifespan Manager]
        Tools[4 Tool Functions]
        Resources[2 Resource Functions]
        Prompts[3 Prompt Functions]
        Crawler[AsyncWebCrawler]

        FastMCP --> Tools
        FastMCP --> Resources
        FastMCP --> Prompts
        Lifespan --> Crawler
        Tools --> Crawler
    end

    subgraph "External"
        Web[Web Pages]
        Browser[Chromium Browser]
    end

    Crawler --> Browser
    Browser --"HTTP / Rendering"--> Web
  `}
  caption="High-level architecture of Crawl4AI-MCP"
/>

## Core Components

### FastMCP v3 Server

Handles transport, MCP protocol plumbing, and registration for tools/resources/prompts.

### Lifespan Manager

Initializes and tears down a shared `AsyncWebCrawler` once per server lifecycle.

### Canonical Tool Surface

| Tool | Purpose |
| :--- | :--- |
| `scrape` | Canonical single/batch page scraping envelope |
| `crawl` | Canonical list/deep traversal envelope |
| `close_session` | Explicit session shutdown |
| `get_artifact` | Artifact retrieval for captured runs |

### Resource Surface

| Resource URI | Description |
| :--- | :--- |
| `config://server` | Server configuration, capabilities, and tool inventory |
| `crawl4ai://version` | Server, crawl4ai, and fastmcp versions |

## Data Flow

<Diagram
  definition={`
sequenceDiagram
    participant Client as MCP Client
    participant Server as FastMCP Server
    participant Tool as Tool Function
    participant Crawler as AsyncWebCrawler
    participant Browser as Chromium
    participant Web as Web Page

    Client->>Server: MCP tool call
    Server->>Tool: Dispatch
    Tool->>Tool: Validate canonical options
    Tool->>Crawler: arun/arun_many
    Crawler->>Browser: Navigate & render
    Browser->>Web: HTTP request
    Web-->>Browser: Response
    Browser-->>Crawler: Rendered content
    Crawler-->>Tool: Crawl results
    Tool->>Tool: Build canonical envelope
    Tool-->>Server: JSON response
    Server-->>Client: MCP response
  `}
  caption="Canonical scrape/crawl execution flow"
/>
